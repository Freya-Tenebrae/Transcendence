generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                          Int @unique @id @default(autoincrement())
  name                        String?
  // surname                    String?
  // nickname                   String @unique
  email                       String @unique
  admin                       Int @default(0)
  password                    Pass?
  // lastConnection             DateTime @default(now())
  // pathAvatar                 String @default("avatar/default_avatar.png") //should be stored to avatar/*user_id* 
  // is_connected               Boolean @default(true)
  // score                      Int @default(0)
  // helo                       Int @default(0)
  // division                   Division @relation(fields: [user_divisionId], references: [division_id])
  // divisionId                 Int @default(0)
}

model Pass {
  user                       User    @relation(fields: [id], references: [id])
  id                         Int     @unique
  salted_password            String?
  salt                       String?
  // oAuth42Link                String// check OAuth42 ???
  // googleAuthLink             String// check GoogleAuth ???
}

// model OAuth42 {

// }

// model GoogleAuth {

// }

// model Channel {
//   id                      Int @unique @id @default(autoincrement())
//   name                    String @unique
//   privacy                 Boolean @default(true)
//   password                String @default("")
//   creation                DateTime @default(now())
// }

// model Division {
//   id                     Int @unique @id @default(autoincrement())
//   name                   String
//   pathImg                String
// }

// model Archivement {
//   id                  Int @unique @id @default(autoincrement())
//   name                String
//   description         String?
//   pathImg             String
// }

// model Match {
//   id                        Int @unique @id @default(autoincrement())
//   user1                     User @relation(fields: [match_userId1], references: [user_id])
//   userId1                   Int
//   user2                     User @relation(fields: [match_userId2], references: [user_id])
//   userId2                   Int
//   // gameMap                   GameMap @relation(fields: [match_gameMapId], references: [gameMap_id])
//   // gameMapId                 Int
//   isRanked                  Boolean
//   score                     Int @default(0)
//   over                      Boolean @default(false)
//   date                      DateTime @default(now())
// }

// // model GameMap {
// //   id                      Int @unique @id @default(autoincrement())
// //   name                    String
// //   pathFile                String
// //   description             String
// // }

// model PowerUp {
//   id                      Int @unique @id @default(autoincrement())
//   name                    String
//   description             String
//   pathIcon                String
//   action                  String
// }

// model Friend {
//   user1                    User @relation(fields: [friend_userId1], references: [user_id])
//   userId1                  Int
//   user2                    User @relation(fields: [friend_userId2], references: [user_id])
//   userId2                  Int
//   accepted                 Boolean @default(false)

//   @@unique([friend_userId1, friend_userId2])
// }

// model Blocked {
//   user1                   User @relation(fields: [blocked_userId1], references: [user_id])
//   userId1                 Int
//   user2                   User @relation(fields: [blocked_userId2], references: [user_id])
//   userId2                 Int

//   @@unique([blocked_userId1, blocked_userId2])
// }

// model IsMemberOf {
//   user                 User @relation(fields: [isMemberOf_userId], references: [user_id])
//   userId               Int
//   channel              Channel @relation(fields: [isMemberOf_channelId], references: [channel_id])
//   channelId            Int
//   status               String @default("INVITED")
//   dateJoined           DateTime @default(now())

//   @@unique([isMemberOf_userId, isMemberOf_channelId])
// }

//   // string acceptable for isMemberOf_status
//   // "CREATOR"
//   // "ADMIN"
//   // "MEMBER"
//   // "INVITED"
//   // "MUTE"
//   // "BAN"

// model ChannelMessage {
//   user             User @relation(fields: [channelMessage_userId], references: [user_id])
//   userId           Int
//   channel          Channel @relation(fields: [isMemberOf_channelId], references: [channel_id])
//   channelId        Int
//   date             DateTime @default(now())
//   content          String

//   @@unique([channelMessage_userId, channelMessage_channelId, channelMessage_date])
// }

// model Private_message {
//   user1            User @relation(fields: [privateMessage_userId1], references: [user_id])
//   userId1          Int
//   user2            User @relation(fields: [privateMessage_userId2], references: [user_id])
//   userId2          Int
//   date             DateTime @default(now())
//   content          String

//   @@unique([privateMessage_userId1, privateMessage_userId2, DateTime])
// }

// model ArchivementDone {
//   user            User @relation(fields: [archivementDone_userId], references: [user_id])
//   userId          Int
//   archivement     User @relation(fields: [archivementDone_userId], references: [user_id])
//   archivementId   Int
//   date            DateTime @default(now())

//   @@unique([archivementDone_userId, archivementDone_archivementId])
// }
