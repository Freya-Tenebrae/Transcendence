import React from "react";
import LoginForm from './LoginForm'
import './sign.css'

const Sign = ({isShowLogin}) => {
	<div className='Sign'>
		return(LoginForm(isShowLogin));
	</div>
};

export default sign;